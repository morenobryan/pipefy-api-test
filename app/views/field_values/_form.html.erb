<%= form_for(field_value) do |f| %>
  <% if field_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(field_value.errors.count, "error") %> prohibited this field_value from being saved:</h2>

      <ul>
      <% field_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :field_id %>
    <%= f.text_field :field_id %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="field">
    <%= f.label :display_value %>
    <%= f.text_field :display_value %>
  </div>

  <div class="field">
    <%= f.label :card_id %>
    <%= f.text_field :card_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
