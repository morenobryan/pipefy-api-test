<%= form_for(pipe) do |f| %>
  <% if pipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pipe.errors.count, "error") %> prohibited this pipe from being saved:</h2>

      <ul>
      <% pipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :token %>
    <%= f.text_field :token %>
  </div>

  <div class="field">
    <%= f.label :can_edit %>
    <%= f.check_box :can_edit %>
  </div>

  <div class="field">
    <%= f.label :organization_id %>
    <%= f.text_field :organization_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
